For reverting commit: (Latest to first Previous)
git checkout <branch>
git reset --hard HEAD~1
git push origin <branch> --force

For cloning to a different repo:
git clone <https-url>
cd <that-repo>
git remote set-url origin <YOUR_NEW_REPOSITORY_URL>
